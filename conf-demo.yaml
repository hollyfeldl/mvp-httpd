# the demo configuration (yaml format)
# rename to "conf.yaml" to run
DocumentRoot: /var/www/html
Listen: 127.0.0.1:8080
TLS: false
TLSListen: 127.0.0.1:8443
TLSCert: /etc/ssl/certs/cert.pem
TLSKey: /etc/ssl/private/key.pem
